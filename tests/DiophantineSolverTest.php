<?php

use Algorithms\Linear\Diophantine;
use Algorithms\Linear\Matrix;
use Algorithms\Linear\MatrixOperator;
use Algorithms\ML\DiophantineSolver;

class DiophantineSolverTest extends PHPUnit_Framework_TestCase
{
    public function testSolver()
    {
//        $equation = new Diophantine(new Matrix(array(array(1,2,3,4))), new Matrix(array(array(30))));
//        $solver = new DiophantineSolver(new MatrixOperator());
//        $result = $solver->solve($equation);
//        self::assertCount(4, $result->toArray());
    }
}